import React from "react";

const Controls = () => {
  return (
    <div>
      <div>
        <button>Play</button>
        <button>Pause</button>
      </div>
    </div>
  );
};

export default Controls;
